//Problem: Sum of all elements in an array.
#include <stdio.h>

int main() {
    int arr[] = {1, 2, 3, 4, 5};
    int n = 5, sum = 0;

    for (int i = 0; i < n; i++) {
        sum += arr[i];  // adds each element
    }

    printf("Sum = %d\n", sum);
    return 0;
}
