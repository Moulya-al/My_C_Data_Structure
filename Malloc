#include <stdio.h>
#include <stdlib.h>

int main() {
    int *arr;
    int n = 3;

    arr = (int*) malloc(n * sizeof(int)); 

    arr[0] = 10;
    arr[1] = 20;
    arr[2] = 30;

    printf("Array elements: %d, %d, %d\n", arr[0], arr[1], arr[2]);

    free(arr); 

    return 0;
}
